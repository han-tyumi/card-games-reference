name: Validate

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          # 22.18+ runs the .ts scripts directly, with no build step.
          node-version: "22.18"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Validate game entries against the schema
        run: npm run validate

      - name: Type-check the tooling
        run: npm run typecheck

      - name: Run the tests
        run: npm test

      - name: Check rendered/ is up to date
        run: npm run render -- --check

      # Builds the whole site and compares it against what is committed. docs/
      # is served straight to readers, so a stale copy is the published rules
      # disagreeing with the source they came from.
      - name: Check docs/ is up to date
        run: npm run web -- --check

      - name: Check the booklet is up to date
        run: npm run pdf -- --check

      - name: Build the PDF
        run: npm run pdf -- --output /tmp/naibi.pdf

      - name: Upload the PDF
        uses: actions/upload-artifact@v4
        with:
          name: naibi-pdf
          path: /tmp/naibi.pdf
