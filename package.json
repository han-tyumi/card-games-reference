{
  "_comment": "Private workspace root. The published package is packages/data, which is named 'naibi'.",
  "name": "naibi-workspace",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A free, openly-licensed reference for how to play traditional and popular card games — written from scratch, stored as structured data, and built to work offline.",
  "license": "(MIT AND CC-BY-SA-4.0)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/han-tyumi/naibi.git"
  },
  "keywords": [
    "card-games",
    "rules",
    "reference",
    "open-data"
  ],
  "engines": {
    "node": ">=22.18"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "validate": "node packages/build/validate.ts",
    "render": "node packages/build/render-markdown.ts",
    "pdf": "node packages/build/build-pdf.ts",
    "web": "node packages/web/build-web.ts",
    "pick": "node packages/build/pick.ts",
    "build": "npm run validate && npm run render && npm run pdf && npm run web",
    "check": "npm run validate -- --quiet && npm run render -- --check && tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "types": "npm run types --workspace naibi"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "@types/pdfkit": "^0.17.6",
    "json-schema-to-typescript": "^15.0.4",
    "typescript": "^7.0.2"
  }
}
